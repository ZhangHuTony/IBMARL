# Balance-specific overlay for RLfD -- the same demos IBMARL bootstraps from.
# Without this, rlfd.yaml's navigation demos (18-dim obs) would be loaded into a
# 16-dim balance buffer.
demonstrations_path: "teachers/balance/demonstrations.pt"
