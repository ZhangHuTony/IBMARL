# Buzz-wire-specific overlay for RLfD -- the same demos IBMARL bootstraps from.
# Without this, rlfd.yaml's navigation demos (18-dim obs)
# would be loaded into a 6-dim buzz_wire buffer.
demonstrations_path: "teachers/buzz_wire/demonstrations_binary.pt"
